export enum TaskStatus {
    pending = 0,
    completed = 1
}
export enum apiPaths {
    Auth = '/auth',
    Tasks = '/tasks'
}
export enum taskGroupMode {
    all = "",
    active = "Active",
    completed = "Completed"
}